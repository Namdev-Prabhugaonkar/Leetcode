class Solution {
public:
    bool rotateString(string A, string B) {
        
        
         if (A.length() != B.length()) 
        return false; 
  
             string temp = A + A;  
             return (temp.find(B) != string::npos); 
    
    }
};
